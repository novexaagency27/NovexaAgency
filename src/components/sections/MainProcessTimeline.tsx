/**
 * NOVEXA — Main Process Timeline Section
 * Pinned sticky scroll timeline detailing the end-to-end client journey.
 */
import React from 'react';
import ProcessPreview from './ProcessPreview';

export const MainProcessTimeline: React.FC = () => {
  return <ProcessPreview />;
};

export default MainProcessTimeline;
